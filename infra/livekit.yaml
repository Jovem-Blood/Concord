port: 7880
logging:
  level: info
rtc:
  # Advertise the Docker Desktop host during local development.
  node_ip: 127.0.0.1
  tcp_port: 7881
  udp_port: 7882
  use_external_ip: false
turn:
  # Direct ICE is sufficient on the same machine. Use the production config remotely.
  enabled: false
